interface:
  display_name: "Refactor"
  short_description: "Behavior-preserving code cleanup"
  default_prompt: "Use $refactor to simplify this code while preserving behavior."
policy:
  allow_implicit_invocation: true
