interface:
  display_name: "React Frontend"
  short_description: "React, routes, styling, and UI tests"
  default_prompt: "Use $react-frontend to build or review a mobile-first React UI change."
policy:
  allow_implicit_invocation: true
